<!DOCTYPE html>
<html>
<head>

    <% if ( broadcaster && broadcaster.name === "rts" ) { %>
        <%- include rts/head %>
    <% } else { %>
        <% include head %>
    <% } %>

</head>
<body>

    <% if ( broadcaster && broadcaster.name === "rts" ) { %>
        <%- include rts/nav %>
    <% } else { %>
        <% include default/nav %>
    <% } %>

    <div class="container">
